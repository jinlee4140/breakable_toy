<!DOCTYPE html>
<html>
<head>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <title>Ball shoes</title>
</head>

<body>
  <%= render 'layouts/navbar' %>
  
  <section id="wrapper" class='clearfix <%= controller_name + '_' + action_name %>'>
    
    <%= yield %>
  </section>

 
  <% if params[:controller] == "homes" ||
      params[:controller] == "users" && params[:action] == "index" ||
  		params[:controller] == "shoes" && params[:action] == "show" ||
  		params[:controller] == "shoes" && params[:action] == "index"
  %>
    <footer><%= render 'layouts/footer' %></footer>
  <% else %>
    <footer class="hacked-footer"><%= render 'layouts/footer' %></footer>
  <% end %>
</body>


</html>